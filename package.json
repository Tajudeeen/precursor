{
  "name": "precursor",
  "version": "0.1.0",
  "private": true,
  "description": "DeFi Attack-Behavior Defense Infrastructure - V1",
  "scripts": {
    "build": "cd packages/shared && npx tsc && cd ../evm && npx tsc && cd ../behavior-engine && npx tsc && cd ../attack-analysis && npx tsc && cd ../simulation && npx tsc && cd ../policy-engine && npx tsc && cd ../../apps/api && npx tsc",
    "test": "vitest run",
    "dev": "vitest --watch",
    "anvil": "anvil -p 8555",
    "deploy:contracts": "cd packages/contracts && forge script Deploy.s.sol:DeployScript --rpc-url http://127.0.0.1:8555 --broadcast --private-key $DEPLOYER_KEY",
    "test:contracts": "cd packages/contracts && forge test",
    "dev:api": "node apps/api/dist/index.js",
    "lint": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.9.0",
    "viem": "^2.56.5",
    "vitest": "^1.6.0",
    "zod": "^3.22.4"
  },
  "dependencies": {
    "@types/express": "^5.0.6",
    "express": "^4.22.3"
  }
}
